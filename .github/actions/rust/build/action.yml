name: "🏗️ Build Rust"
description: "Build aplicação Rust"

inputs:
  profile:
    description: "Build profile (debug, release)"
    required: false
    default: "release"

runs:
  using: "composite"
  steps:
    - name: "Build"
      shell: bash
      run: |
        if [ "${{ inputs.profile }}" = "release" ]; then
          cargo build --release
        else
          cargo build
        fi
